name: dia_router
description: A router for Dia. Allow add dia middlewares for handle different url addresses.
version: 0.1.7
homepage: https://github.com/unger1984/dia

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  dia: ^0.1.3
  path_to_regexp: ^0.4.0

dev_dependencies:
  dart_code_metrics: ^5.6.0
  lints: ^2.0.1
  test: ^1.23.1
  http: ^0.13.5
